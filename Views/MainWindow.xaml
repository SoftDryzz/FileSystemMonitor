<Window x:Class="FileSystemMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Monitor de Archivos"
        Height="450"
        Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TÍTULO -->
        <TextBlock Text="📁 Monitoreo de Archivos"
                   FontSize="20"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <!-- LISTA DE EVENTOS REGISTRADOS -->
        <ListBox Name="LogListBox"
                 Grid.Row="1"
                 Margin="5"/>

        <!-- BOTONES DE CONTROL -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <CheckBox Name="UserOnlyCheckBox" Content="Mostrar solo cambios del usuario" 
              Margin="25" IsChecked="True"/>
            <Button Name="StartButton"
                    Content="Iniciar Monitoreo"
                    Click="StartButton_Click"
                    Width="150"
                    Height="40"
                    Margin="5"/>

            <Button Name="StopButton"
                    Content="Detener Monitoreo"
                    Click="StopButton_Click"
                    Width="150"
                    Height="40"
                    Margin="5"/>

            <Button Name="ClearLogButton"
                    Content="Borrar Registro"
                    Click="ClearLogButton_Click"
                    Width="150"
                    Height="40"
                    Margin="5"/>
        </StackPanel>
    </Grid>
</Window>